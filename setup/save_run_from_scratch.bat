REM
echo Skipping save_run_from_scratch under windows
REM DESTINATION=${1:-~/elexis/rcptt/rcptt_result.h2.db}
REM mkdir -p `dirname ${DESTINATION}`
REM logger "Saved:  found:  `ls -l /tmp/elexis*h2.db`"
REM cp -pv /tmp/elexis*h2.db ${DESTINATION}
REM logger "Saved: $0 ${DESTINATION}"
